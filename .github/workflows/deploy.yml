name: Keep Bot Alive
on:
  schedule:
    - cron: '0 */6 * * *'  # كل 6 ساعات
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway Service
        run: |
          curl -I https://your-railway-url.railway.app || true
